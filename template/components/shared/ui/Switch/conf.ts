import { COLORS } from "@/constants/Colors";
import { theme } from "@/utils/getTheme";

export const DEFAULT_WIDTH = 52;
export const DEFAULT_HEIGHT = 30;
export const DEFAULT_ON_COLOR = COLORS[theme].Surface.action;
export const DEFAULT_OFF_COLOR = COLORS[theme].Surface.actionDisabled;
export const DEFAULT_THUMB_COLOR = COLORS[theme].Surface.primary;
export const DEFAULT_THUMB_INSET = 3;
